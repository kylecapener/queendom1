generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Contact {
  id        Int       @id @default(autoincrement())
  name      String
  phone     String
  notes     String    @default("")
  payments  Payment[]
  messages  Message[]
  createdAt DateTime  @default(now())
  updatedAt DateTime  @updatedAt
}

model Message {
  id        Int      @id @default(autoincrement())
  contactId Int
  contact   Contact  @relation(fields: [contactId], references: [id], onDelete: Cascade)
  direction String   // "outbound" | "inbound"
  body      String
  sentBy    String   @default("") // admin username for outbound
  createdAt DateTime @default(now())
}

model Payment {
  id        Int      @id @default(autoincrement())
  contactId Int
  contact   Contact  @relation(fields: [contactId], references: [id], onDelete: Cascade)
  amount    Float
  date      DateTime
  note      String   @default("")
  createdAt DateTime @default(now())
}
